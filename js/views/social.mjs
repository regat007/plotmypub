// PlotMyPub — Social tab (placeholder). Future home of groups, invites, members.
import { placeholderView } from '../router.mjs';

placeholderView('social', {
  emoji: '👥',
  title: 'Social',
  blurb: '-- Coming soon! --',
});
