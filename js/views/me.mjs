// PlotMyPub — Me tab (placeholder). Future home of the personal profile.
import { placeholderView } from '../router.mjs';

placeholderView('me', {
  emoji: '🙂',
  title: 'Me',
  blurb: 'Your profile\n -- Coming soon! --'
});
